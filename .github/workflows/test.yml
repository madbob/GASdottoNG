name: Tests
on: [push]

jobs:
  phpunit:
    name: PHPUnit
    runs-on: ubuntu-latest
    steps:
    - run: cd code
    - uses: actions/checkout@v1
    - uses: nathanheffley/laravel-phpunit-action@master
